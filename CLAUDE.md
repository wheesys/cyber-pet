项目为ai桌面宠物项目
宠物形象可以包括各种动物，二次元人物形象等等，注意版权问题。
需要支持mac/win/linux/android/iphone
各个端的图形渲染等尽量使用原生代码，客户端项目性能第一。
每个电脑可以启动一个客户端，每个客户端可以生成多个宠物，单客户端内的多个宠物可以互动。
多个客户端通过p2p互相连接，连接后可以a客户端的宠物可以去b客户端的桌面和b客户端的宠物互动，回来的时候还可以给a客户端的用户带一份礼物-记得提醒用户查收礼物。礼物甚至可以是a客户端的宠物自己创建的一份空白文件。
是否允许客户端通过p2p连入由用户选择，默认关闭。
可以预设宠物动作模型，由ai给出宠物行为，语言。
宠物的性格可以预设多个模板，比如古灵精怪，文静，沉稳等等。但是要注意限度，不能真的对用户设备，用户文件产生损失。玩笑可以是比如在用户桌面创建三五个空文件，数量也要严格控制。
用户可以通过宠物向其他宠物求助，比如寻找某个软件，或者某个问题怎么处理，在线的其他客户端的宠物可以询问用户是否需要帮助，如果帮助的话，帮助过程中的比如寻找文件，查看位置等信息都需要用户确认。
宠物可以帮助用户整理电脑，修复问题，安装软件-或寻找官方软件源。但是相关权限要严格限制，每次操作之前都需要用户确认。无法完成的操作要告诉用户具体怎么操作。
软件本身要足够绿色，少依赖，尽量做到删除软件所在的目录就能彻底卸载干净。
ai行为不复杂，优先接入低成本ai, 可以保留本地配置方案，但软件本身不提供本地模型。
ai供应商配置的时候，直接声明简单问题ai(deepseek,qwen模型)和复杂问题ai(claude, gpt)以及综合调度ai.供应商配置要支持用户自定义请求基础地址。
由综合调度ai判断问题是复杂还是简单，协助用户进行复杂问题处理的时候自动切换到复杂问题ai。
可以有基础的账户体系，同账户的多个设备之间宠物互动的更加频繁，同账户的a设备可以给b设备上的宠物授权。
宠物行为要考虑显示设备的窗口大小。进出其他设备时要有进出动画。
